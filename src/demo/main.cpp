#include <core/core.h>

#include "core/window/window.h"

using namespace bombengine;

int main()
{
    std::shared_ptr<Core> core = Core::Initialize();

    std::shared_ptr<Window> window = core->

    core->Run();

    return 0;
}
